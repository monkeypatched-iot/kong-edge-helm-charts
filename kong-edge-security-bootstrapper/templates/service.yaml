apiVersion: v1
kind: Service
metadata:
  name: {{ include "kong-security-bootstrapper.fullname" . }}
  labels:
    {{- include "kong-security-bootstrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kong-security-bootstrapper.selectorLabels" . | nindent 4 }}
