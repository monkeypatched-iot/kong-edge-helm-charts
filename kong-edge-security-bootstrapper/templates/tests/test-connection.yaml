apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong-security-bootstrapper.fullname" . }}-test-connection"
  labels:
    {{- include "kong-security-bootstrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kong-security-bootstrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
